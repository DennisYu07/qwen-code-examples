version: '3.8'

services:
  qwen-bolt:
    build:
      context: .
      dockerfile: Dockerfile
    
    container_name: qwen-bolt-demo
    
    ports:
      - "3000:3000"
    
    environment:
      - NODE_ENV=production
      - PORT=3000
    
    # 自动重启策略
    restart: unless-stopped
